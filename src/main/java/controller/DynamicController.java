package controller;

public class DynamicController {
    // byte 를 받아와서 byte 반환하는 형태가 괜찮은가?
    public static byte[] runDynamicFactory(byte[] body){
        byte[] manualBody;
        manualBody = new byte[0];
        return manualBody;
    }
}
